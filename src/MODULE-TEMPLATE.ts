/**
 * MODULE TEMPLATE
 * All Mind modules must:
 * - Accept inputs as contracts (from alive-constitution)
 * - Return outputs as contracts
 * - NEVER call actuators, tools, or external services
 * - NEVER enforce policy
 */
export class YourModuleName {
  process(_input: unknown): unknown {
    // TODO: implement
    return null;
  }
}
