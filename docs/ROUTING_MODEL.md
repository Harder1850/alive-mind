# Routing Model

## Inbound
External Input → Body Sensors → Runtime Router → UC → STM

## Outbound
STM → Decision Formation → Decision Contract → Runtime → Body Actuators

## Memory
STM → LTG → LTM (write) | LTM → Activation Engine → STM (recall)

## Rules
- Mind never calls Body directly
- All routing goes through Runtime bridges
